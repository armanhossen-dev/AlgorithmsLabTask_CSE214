#include<stdio.h>
#include<stdlib.h>
//you are given a sorted array of numbers. use binary search to find the position of a specific number. number is not in the array, return -1.

typedef struct b{
    int data;
    bst *left;
    bst *right;
}bst;


int main(){
    //sorted array?
    // a collection of elements arranged in a specific order, usually in ascending or descending order
    //Ascending order: [1, 2, 3, 4, 5]
    //Descending order: [5, 4, 3, 2, 1]

    //take array size
    int size; //howmany number will take
    scanf("%d",&size);
    for(int i = 1; i<= size; i++){
        
    }


    return 0;
}