// Find the leader Elements
// leader element in an array is greater than all the elements to its right ,
#include <stdio.h>
int main()
{
    int num[5];
    for (int i = 0; i < 5; i++)
    {
        scanf("%d", &num[i]);
    }
    int target = num[0];
    for (int i = 0; i < 5; i++){
        for (int j = i; j < 5; j++){
        
        }
    }
return 0;
}